import styled from "styled-components/macro";

const MaxWidthWrapper = styled.div`
  max-width: 100%;
  margin-left: auto;
  margin-right: auto;
  width: var(--page-width-with-gap);
  padding-left: var(--page-gap);
  padding-right: var(--page-gap);
`;

export default MaxWidthWrapper;
